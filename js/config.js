/**
 * ==========================================================================
 * Supabase 환경 설정 파일 (js/config.js)
 * Vercel 배포 및 로컬 실행 시 Supabase 데이터베이스 접속 정보를 설정합니다.
 * ==========================================================================
 */

window.SUPABASE_CONFIG = {
  // Supabase 대시보드 -> Project Settings -> API에서 확인한 Project URL 입력
  url: "",
  
  // Supabase 대시보드 -> Project Settings -> API에서 확인한 anon public key 입력
  anonKey: ""
};
